resources:
  postgres_projects:
    workshop_zerobus_lakebase:
      project_id: "zerobus-lakebase-${workspace.current_user.id}"
      display_name: "Zerobus Lakebase Workshop — ${workspace.current_user.name.givenName} ${workspace.current_user.name.familyName}"
      pg_version: 17
      default_endpoint_settings:
        autoscaling_limit_min_cu: 0.5
        autoscaling_limit_max_cu: 2
        suspend_timeout_duration: 300s
